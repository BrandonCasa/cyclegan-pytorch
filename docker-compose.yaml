services:
  app:
    build: .
    shm_size: '6gb'
    container_name: nti-alias
    ports:
      - 3001:3000
    volumes:
      - .:/app
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              capabilities: [gpu]